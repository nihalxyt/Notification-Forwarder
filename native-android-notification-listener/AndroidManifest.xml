<!--
  Add these declarations to your android/app/src/main/AndroidManifest.xml
  after running `npx expo prebuild --platform android`.
-->

<!-- PERMISSIONS: Add inside <manifest> tag, before <application> -->
<!--
<uses-permission android:name="android.permission.RECEIVE_SMS" />
<uses-permission android:name="android.permission.READ_SMS" />
-->

<!-- COMPONENTS: Add inside <application> tag -->

<!-- Notification Listener Service (captures bKash/NAGAD/Rocket app & SMS notifications) -->
<!--
<service
    android:name=".PaymentNotificationListenerService"
    android:label="Paylite Payment Listener"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="false">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>
-->

<!-- SMS Broadcast Receiver (direct SMS reading as backup method) -->
<!--
<receiver
    android:name=".PaymentSmsReceiver"
    android:exported="true"
    android:permission="android.permission.BROADCAST_SMS">
    <intent-filter android:priority="999">
        <action android:name="android.provider.Telephony.SMS_RECEIVED" />
    </intent-filter>
</receiver>
-->
